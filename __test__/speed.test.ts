import {is as thiis} from '../dist';
import * as alternative from 'is';

describe('Speed Test', () => {
    it('Speed Test is.string()', () => {
        // let variable = {a: 1};
        // console.time('thiis.object.not.empty()');
        // for (let i = 0; i < 10_000; i++) {
        //     if (thiis.object.not.empty(variable)) {
        //
        //     }
        // }
        // console.timeEnd('thiis.object.not.empty()');
        // console.time('thiis.object() thiis.not.empty()');
        // for (let i = 0; i < 10_000; i++) {
        //     if (thiis.object(variable) && thiis.not.empty(variable)) {
        //
        //     }
        // }
        // console.timeEnd('thiis.object() thiis.not.empty()');
        // console.time('object.not.empty');
        // for (let i = 0; i < 10_000; i++) {
        //     if (typeof variable === 'object' && variable !== null && !Array.isArray(variable) && Object.keys(variable)?.length) {
        //     }
        // }
        // console.timeEnd('object.not.empty');
        // console.time('is.object.not.empty()');
        // for (let i = 0; i < 10_000; i++) {
        //     if (alternative.object(variable) && !alternative.empty(variable)) {
        //
        //     }
        // }
        // console.timeEnd('is.object.not.empty()');
        expect(true).toBeTruthy();
    });

});
